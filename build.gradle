

buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        // Note for FTC Teams: Do not modify this yourself.
        classpath 'com.android.tools.build:gradle:8.7.0'
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

repositories {
    mavenCentral()
}

tasks.register('visualizer') {
    group = 'pedro visualizer'
    description = 'Start the Pedro visualizer and open it in your browser.'
    dependsOn ':PedroVisualizer:visualizer'
}
